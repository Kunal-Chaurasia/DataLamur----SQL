SELECT user_id, spend, transaction_date
from (SELECT user_id, spend, transaction_date, row_number() over (partition by user_id order by transaction_date) as row_num FROM transactions) as table_row
where row_num = 3;