select d.department_name, s.name, s.salary from (
SELECT department_id, name, salary, dense_rank() over (partition by department_id order by salary desc) as ranking FROM employee) as s
inner join department as d
on s.department_id = d.department_id
where s.ranking <= 3
order by d.department_name asc, s.salary desc, s.name asc;